import { FormControl } from '@angular/forms';

export default function comparePasswords(password1: any, password2: any): any {

    console.log("dede")
    // if(password1.value === password2.value) {
    //     return null;
    // } else {
    //     return 
    // }
    return null
}